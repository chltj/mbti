<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.k_mbti.dao.InquiryDao">

    <!-- 전체 목록 -->
    <select id="findAll" resultType="com.example.k_mbti.dto.InquiryDto">
        SELECT
            id,
            title,
            content,
            writer,
            created_at AS createdAt
        FROM inquiry
        ORDER BY id DESC
    </select>

    <!-- 저장 -->
    <insert id="insertInquiry" parameterType="com.example.k_mbti.dto.InquiryDto">
        INSERT INTO inquiry (title, content, writer, created_at)
        VALUES (#{title}, #{content}, #{writer}, NOW())
    </insert>

    <!-- 단건 조회 -->
    <select id="findById" parameterType="long"
            resultType="com.example.k_mbti.dto.InquiryDto">
        SELECT
            id,
            title,
            content,
            writer,
            created_at AS createdAt
        FROM inquiry
        WHERE id = #{id}
    </select>

</mapper>
